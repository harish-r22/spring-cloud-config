spring:
  cloud:
    config:
      uri: http://localhost:8888/
      profile: prod

management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - refresh
  endpoint:
    health:
      show-details: always
health:
  config:
    enabled: true
